{
  "name": "babel-plugin-version-transform",
  "version": "2.0.0",
  "description": "A Babel plugin for injecting package version numbers.",
  "main": "lib",
  "scripts": {
    "build": "rimraf lib && babel ./src -d lib",
    "prepublish": "npm run build",
    "release": "node ./scripts/release.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blainekasten/babel-plugin-version-transform.git"
  },
  "dependencies": {
    "@babel/preset-env": "^7.0.0",
    "rimraf": "^2.5.2"
  },
  "devDependencies": {
    "@babel/core": "7.x",
    "@babel/cli": "7.x",
    "readline-sync": "^1.4.1"
  },
  "peerDependencies": {
    "@babel/core": "7.x",
    "@babel/cli": "7.x"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "author": "Blaine Kasten<blainekasten@gmail.com>",
  "license": "MIT"
}
